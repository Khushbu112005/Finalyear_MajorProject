{
  "name": "civicsphere-ai",
  "version": "1.0.0",
  "private": true,
  "description": "CivicSphere AI Master Production Workspace",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "lint": "npm run lint --prefix apps/web"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
